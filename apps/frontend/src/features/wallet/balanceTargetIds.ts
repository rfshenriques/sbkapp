/** DOM id given to the header's freebets BalancePills instance - the fly animation's landing target, shared between AppShell (renders it), FreebetCreditedModal (triggers the fly toward it), and FreebetFlyOverlay (reads its bounding rect). */
export const HEADER_FREEBETS_BALANCE_ID = 'header-freebets-balance';
